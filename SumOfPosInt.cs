using System;
using System.Collections;
using System.Security.Cryptography.X509Certificates;

class SumOfPosInt
{
    public static void Main()
    {
       ArrayList arr = new ArrayList();
       
    }

    static int squareSumFunc(int a, int b)
    {
        return ((a*a) + (b*b));
    }
}